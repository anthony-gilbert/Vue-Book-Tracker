apiVersion: v1
kind: Service
metadata:
  name: {{ include "book-tracker.fullname" . }}
  labels:
    {{- include "book-tracker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "book-tracker.selectorLabels" . | nindent 4 }}
